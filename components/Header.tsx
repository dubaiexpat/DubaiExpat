// Navigation is now handled by the Sidebar component (components/Sidebar.tsx)
// which is injected globally via app/layout.tsx.
// This component is kept for backward compatibility with existing page imports.
export default function Header() {
  return null;
}
